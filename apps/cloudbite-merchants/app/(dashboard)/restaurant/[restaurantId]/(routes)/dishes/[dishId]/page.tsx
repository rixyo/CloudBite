import React from 'react';

type pageProps = {
  params: {
    restaurantId: string;
  };
};

const page:React.FC<pageProps> = ({params}) => {

    return (
        <div>

        </div>
    )
}
export default page;